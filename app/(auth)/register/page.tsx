import React from 'react';
import Layout from '../../layout';
import Register from './register';

const page: React.FC = () => {
  return (
    <Layout>
        <Register/>
    </Layout>
  );
};

export default page;
